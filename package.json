{
    "name": "no-slash",
    "version": "1.2.10",
    "description": "express middleware to remove trailing slash",
    "main": "lib/index.js",
    "author": "Brian Ruddy <briancruddy@gmail.com>",
    "license": "MIT",
    "engines": {
        "node": ">=6"
    },
    "scripts": {
        "start": "node -e 'require(\"./test/app\").listen(3002)'",
        "test2": "nyc -a --branches 100 --functions 100 --lines 100 --statements 100 --check-coverage --reporter html --exclude coverage --exclude test mocha test",
        "test": "jest --config=jest.config.json --no-cache",
        "pretest": "eslint lib/*.js test/*.js"
    },
    "repository": {
        "type": "git",
        "url": "https://github.com/bcruddy/no-slash"
    },
    "devDependencies": {
        "eslint": "^3.19.0",
        "express": "^4.15.2",
        "jest": "^20.0.4",
        "supertest": "^3.0.0"
    },
    "dependencies": {
        "url": "^0.11.0"
    }
}
